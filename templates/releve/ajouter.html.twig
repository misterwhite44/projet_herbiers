{% extends 'base.html.twig' %}

{% block title %}Ajouter un Relevé{% endblock %}

{% block body %}
    <div class="container mt-5">
        <div class="row justify-content-center">
            <div class="col-md-6">
                <h2 class="text-center mb-4">Ajouter un Relevé</h2>
                <div class="text-center">
                    {{ include('releve/_formulaire.html.twig', {'button_label': 'Ajouter', 'form_method': 'POST', 'form_action': path('app_releve_ajouter') }) }}
                </div>
            </div>
        </div>
    </div>
{% endblock %}
